reset;
model sudoku.mod;
data sudoku.dat;
# show;
# expand;
option solver cplex;

solve;

#display _solve_elapsed_time;

printf {i in N, j in N, k in M} "%s,%s,%s,%i\n", i, j,k, x[i,j,k] > "x_matrix.csv";